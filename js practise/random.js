"use strict"
// function calcAge(birthyear){
//     const age=2037-birthyear;
//     function printage(){
//         const output=`${firstname},you are ${age}, born in ${birthyear}`;
//         console.log(output);
//     }

//     printage();
//     return age;
// }
// const firstName='joans';
// printage();

// 

// const random =[6,2,3,4,3,2,1,'sham','samara','john','samara','sham','krishna'];

// console.log(random.length)
// let sort_elements=random.sort();
// // console.log(sort_elements);
//  let remove_repeated=new Set(sort_elements);
//  console.log(remove_repeated);


// const arr =[6,2,3,4,3,2,1,'sham','samara','john','samara','sham','krishna'];

 
// function compare(a, b) {
//     const typeA = typeof a; // type a number  and type a  is also  string  in case the arr contains s
    

//     const typeB = typeof b; // type b is  string 
//     // console.log(typeB);

//     if (typeA === typeB) {   // type typeA is "number" and typeB is "string", they are not equal, so the else block is executed.
    

//         // If same type, compare values directly
//         return a > b ? 1 : -1;
//     } else {

//         // If different types, sort by data type
//         return typeA > typeB ? 1 : -1;
//     }
// }
// let sort_elements=arr.sort(compare);
// console.log(sort_elements);
//  let remove_repeated=new Set(sort_elements);
//  console.log(remove_repeated);






